# Security

Do not include setup photographs, raw WebSocket messages, HomeKit pairing data,
network credentials, MACs, or local topology in public reports. Describe the
command, package/firmware version, and sanitized error. Local controller access
is trusted-network access; do not expose its web server to the internet.
